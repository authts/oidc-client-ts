{
  "name": "Parcel",
  "scripts": {
    "start": "concurrently --kill-others \"npm run parcel\" \"npm run server\"",
    "parcel": "parcel serve ./src/index.html --no-cache",
    "server": "node server.js",
    "build": "parcel build ./src/index.html"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^6.2.1",
    "parcel": "^2.0.0-rc.0"
  }
}
